<h1>Hangman</h1>

<ul>
  <% @games.each do |game| %>
    <li>
      <h2> 
        <%= link_to "Game #{game.id}", game_path(game) %> - <%= game.status %>
      </h2>
    </li>
  <% end %> 
</ul>

<%= link_to 'start a new game', new_game_path %>